namespace Doctor
{
    public class Referrals
    {
        public string Referral { get; private set; }

        public Referrals(string referral)
        {
            Referral = referral;
        }
    }
}
